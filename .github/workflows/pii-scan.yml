# PII / secret leak scan.
#
# Ported from the desktop client (dbboard ADR-0055 / ADR-0084). Both repos are
# public under the same account and share the same exposure: fixtures full of
# connection strings, and commit metadata nobody was checking.
#
# `scripts/pii-scan.sh` runs locally on every commit (pre-commit + commit-msg
# hooks); this workflow runs the same scanner in CI on every push/PR to the
# integration branches, plus once a day so a leak that lands out-of-band (a
# merge, a direct push, a rebase) is caught within 24h even if nobody commits.
#
# What it scans:
#   * `--selftest`   — proves the rules still fire (guards against a broken edit).
#   * `--tree`       — tracked files at HEAD: blocking rules + denylist fail the
#                      job; the high-false-positive shape rules print as advisory.
#   * `--range`      — commit MESSAGES. This repo's `main` is the root commit
#                      (`develop` is the integration branch), so `origin/main..HEAD`
#                      is effectively every message — ~130 today, all verified
#                      clean, and cheap enough to keep scanning in full.
#   * `--identity`   — author/committer of the commits this push/PR introduced.
#                      Unlike a leaked string in a file, an address in a commit
#                      object cannot be fixed by a later commit.
#
# The identity step is deliberately event-scoped, NOT full-history: every commit
# before this workflow landed carries a personal author address, pending the
# one-time rewrite (docs/maintainer/pii-scanning.md § History rewrite). A
# full-history identity scan would be permanently red and drown the signal.
# This step exists to stop NEW ones.
#
# The denylist (real names / maintainer PII, never committed) is materialized
# from the `PII_DENYLIST` repo secret into an untracked file for the run. When
# the secret is absent (forks, first-run) the scan degrades to generic rules
# only rather than failing. CI runs WITHOUT `--reveal`: matches are redacted so
# a public Actions log never echoes the very strings we hide.
#
# Least privilege: `contents: read` only, no write scope, no third-party action
# (only first-party `actions/checkout`, pinned by major tag). The scanner is a
# POSIX sh script in-repo; no untrusted code is built or executed.
name: pii-scan

on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [develop, main]
  schedule:
    # 06:00 UTC daily (~15:00 JST).
    - cron: "0 6 * * *"
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: pii-scan-${{ github.ref }}
  cancel-in-progress: true

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout (full history for commit-message range scan)
        uses: actions/checkout@v4
        with:
          # Need history so `origin/main..HEAD` resolves; the scan itself only
          # reads messages in that range, not the full tree history.
          fetch-depth: 0
          # No git operation here needs the token stored in .git/config.
          persist-credentials: false

      - name: Materialize denylist from secret (if present)
        env:
          PII_DENYLIST: ${{ secrets.PII_DENYLIST }}
        run: |
          if [ -n "$PII_DENYLIST" ]; then
            printf '%s\n' "$PII_DENYLIST" > .pii-denylist
            echo "denylist: materialized from secret"
          else
            echo "denylist: PII_DENYLIST secret absent — generic rules only"
          fi

      - name: Selftest
        run: sh scripts/pii-scan.sh --selftest

      - name: Scan tracked files at HEAD
        run: sh scripts/pii-scan.sh --tree

      - name: Scan commit messages
        run: |
          git fetch --no-tags origin main >/dev/null 2>&1 || true
          if git rev-parse --verify -q origin/main >/dev/null; then
            RANGE="origin/main..HEAD"
          else
            RANGE="HEAD~20..HEAD"
          fi
          echo "range: $RANGE"
          sh scripts/pii-scan.sh --range "$RANGE"

      - name: Scan commit identity of commits new on this ref
        env:
          BEFORE: ${{ github.event.before }}
          AFTER: ${{ github.sha }}
          PR_BASE: ${{ github.event.pull_request.base.sha }}
          PR_HEAD: ${{ github.event.pull_request.head.sha }}
        run: |
          # Only commits this event actually introduced — see the header note
          # on why a wider range would be permanently red.
          if [ -n "${PR_BASE:-}" ] && [ -n "${PR_HEAD:-}" ]; then
            RANGE="$PR_BASE..$PR_HEAD"
          elif [ -n "${BEFORE:-}" ] && [ "$BEFORE" != "0000000000000000000000000000000000000000" ]; then
            RANGE="$BEFORE..$AFTER"
          else
            echo "identity: no push/PR range for this event — skipping"
            exit 0
          fi
          git rev-parse --verify -q "${RANGE%%..*}" >/dev/null || {
            echo "identity: base commit not present in this checkout — skipping"
            exit 0
          }
          echo "range: $RANGE"
          sh scripts/pii-scan.sh --identity "$RANGE"

      - name: Shred denylist
        if: always()
        run: rm -f .pii-denylist
