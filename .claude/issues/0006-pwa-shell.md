# 0006 — PWA shell (Phase 1.5)

- **Status:** open
- **Phase:** 1.5
- **Opened:** 2026-05-26
- **Closed:** —
- **Branch:** _to be created_ (`feature/phase-1.5-pwa-shell` suggested)
- **Depends on:** [0002](./0002-monorepo-scaffold.md) (monorepo scaffold)
- **Blocks:** _none — runs in parallel with `0003`/`0004`/`0005`_

## Goal

Make the Nuxt app (`apps/web`) installable as a Progressive Web App so a self-hosted `dbboard-web` instance can be added to a phone's home screen for ambient, read-mostly access (think GitHub mobile app: glance at production signup counts during a meeting, verify a specific order, kill a long-running query from a phone).

Strategic context lives in [`handoff/2026-05-26-pwa-pivot-incoming.md`](../handoff/2026-05-26-pwa-pivot-incoming.md):

- No native Android / iOS apps. No `dbboard-mobile` repo.
- PWA-ify `dbboard-web` instead. One codebase covers laptop browsers and mobile install.
- Forward-compatible with desktop ADR-0011 (HTTP contract = public API) and ADR-0012 (Capability pattern). No contract churn.

This phase touches the application shell only — manifest, service worker, responsive layout, install prompt. It does **not** depend on the HTTP contract, so it proceeds in parallel with the wait on desktop's `/capabilities` publication (which gates `0003`/`0004`/`0005`).

## Scope

1. `apps/web/public/manifest.json` — `name`, `short_name`, `icons` (192×192 and 512×512, both with a `purpose: "maskable"` variant), `start_url`, `display: standalone`, `theme_color`, `background_color`.
2. Nuxt PWA module (`@vite-pwa/nuxt`) — auto-generated service worker, Workbox precache, an explicit offline fallback page (no white screen on cold offline start).
3. Install prompt UI — capture `beforeinstallprompt`, surface as opt-in (e.g. a small "Install" affordance in settings or a one-time inline hint). Never auto-push the dialog.
4. iOS Safari support — `apple-touch-icon`, `apple-mobile-web-app-capable`, `apple-mobile-web-app-status-bar-style`. iOS 16.4+ exposes Web Push, but Web Push itself is out of scope (see below).
5. Mobile-first responsive review of the current Nuxt smoke page so the responsive baseline is in place before Phase 4 builds real UI on top of it. At 375 × 667: schema sidebar collapses, SQL editor stacks vertically, results table scrolls horizontally, touch targets are ≥ 44 × 44 px.
6. Icon asset pipeline — generate the required icon sizes from a single source SVG via `@vite-pwa/assets-generator` (or equivalent), checked into `apps/web/public/`.

## Out of scope

- **Web Push notifications.** Optional follow-up, not part of this issue. When picked up later: VAPID key generated locally + the `web-push` Node library for self-hosted delivery. No vendor lock-in.
- **Authentication.** PWA installability does not require auth.
- **Offline write queue.** The read-mostly assumption makes this non-essential. The offline fallback is read-only.
- **Native apps / `dbboard-mobile` repo.** Revisit only after PWA demand is observable in practice.

## Tasks

- [ ] Add `@vite-pwa/nuxt` and `@vite-pwa/assets-generator` as dev dependencies in `apps/web/package.json` (respect `minimumReleaseAge` + `onlyBuiltDependencies` ACL).
- [ ] Source SVG icon checked into `apps/web/public/icons/source.svg` (or similar) and asset-generator config emitting 192/512 + maskable variants.
- [ ] `apps/web/public/manifest.json` written; verify it via Chrome DevTools Application tab.
- [ ] Configure `@vite-pwa/nuxt` in `apps/web/nuxt.config.ts` — register mode, workbox precache, offline fallback route.
- [ ] Offline fallback page at `apps/web/pages/offline.vue` (or framework equivalent) with an explicit "you are offline" message and the cached shell.
- [ ] iOS meta tags wired in the root layout / `app.vue` head config.
- [ ] Install prompt composable (`useInstallPrompt` or similar) that stashes `beforeinstallprompt` and exposes an opt-in trigger.
- [ ] Responsive baseline pass on the existing smoke page — verify at 375 × 667 mobile viewport.
- [ ] Tests: unit test the install-prompt composable; a Vitest snapshot or assertion on the generated manifest shape.
- [ ] Manual verification matrix recorded in the PR description: Android Chrome installability, iOS Safari standalone launch, Lighthouse PWA score, offline cold start.

## Definition of Done

Per [`.claude/roadmap.md` Phase 1.5](../roadmap.md):

- [ ] `apps/web/public/manifest.json` present with `name`, `icons` (192×192 and 512×512 maskable), `start_url`, `display: standalone`, `theme_color`.
- [ ] Service worker auto-generated by `@vite-pwa/nuxt` with precache and an explicit offline fallback page.
- [ ] Install-prompt UI is opt-in (captures `beforeinstallprompt`, never auto-pushes).
- [ ] iOS Safari meta tags configured (`apple-touch-icon`, `apple-mobile-web-app-capable`, status-bar style).
- [ ] Android Chrome shows "Add to Home Screen" (installability check passes).
- [ ] iOS Safari launches in standalone display mode with the icon on the home screen.
- [ ] Lighthouse PWA score ≥ 90.
- [ ] Mobile viewport 375 × 667 leaves the smoke page usable without layout breakage.
- [ ] Cold start while offline shows the cached UI shell + an explicit "offline" message.
- [ ] `pnpm -r typecheck`, `pnpm -r lint`, `pnpm -r test`, `pnpm -r build` all green.
- [ ] [`.claude/project-status.md`](../project-status.md) updated to mark Phase 1.5 done.

## Handback

When the top three Acceptance items (Android installability, iOS standalone, Lighthouse ≥ 90) pass, send a short report back to desktop via a `chore(handoff): ...` brief in `.claude/handoff/`. Empirical feedback on whether ambient + read-mostly UX actually works on real devices is welcome — that evaluation determines whether "native is still needed" or "PWA is enough" gets locked in for the long term.

## References

- Incoming brief: [`../handoff/2026-05-26-pwa-pivot-incoming.md`](../handoff/2026-05-26-pwa-pivot-incoming.md)
- Phase definition: [`../roadmap.md`](../roadmap.md) → Phase 1.5
- `@vite-pwa/nuxt` docs (third-party): https://vite-pwa-org.netlify.app/frameworks/nuxt

## Work log

### 2026-06-03 — code-side implementation landed on `feature/phase-1.5-pwa-shell`

- `@vite-pwa/nuxt@^1.1.0` + `@vite-pwa/assets-generator@^1.0.0` added as `apps/web` devDependencies. `sharp` added to the build-script allowlist in `pnpm-workspace.yaml` with reason (build-time only, used by the assets generator).
- Manifest defined in `apps/web/app/pwa/manifest.ts` and consumed by both `nuxt.config.ts` and the unit tests so DoD fields cannot silently regress.
- Icon assets generated from `apps/web/public/icons/source.svg` (minimal-2023 preset): `pwa-64x64.png`, `pwa-192x192.png`, `pwa-512x512.png`, `maskable-icon-512x512.png`, `apple-touch-icon-180x180.png`, `favicon.ico`. All checked in so the production build does not depend on the `sharp` binary being present.
- App split into `app/app.vue` (shell + head + responsive baseline) and `app/pages/index.vue` (smoke). New `app/pages/offline.vue` is the Workbox `navigateFallback`.
- iOS Safari meta tags (`apple-mobile-web-app-capable`, status-bar style, `apple-touch-icon`) wired via `useHead` in `app/app.vue`.
- `useInstallPrompt` composable captures `beforeinstallprompt` and exposes opt-in `prompt()`. Mini-infobar never auto-fires; the module's built-in install banner is disabled (`client.installPrompt: false`).
- Mobile-first CSS pass on the shell: 1rem padding at < 768px, install button is 44 × 44 minimum, safe-area insets respected for the iOS standalone status bar.
- Tests: `tests/pwa-manifest.test.ts` asserts DoD-required manifest fields and icon shape; `tests/useInstallPrompt.test.ts` exercises capture → prompt → outcome → reset and the `appinstalled` reset, all under `@vue/test-utils` mounted into `happy-dom`.
- Verification chain green: `pnpm format:check`, `pnpm -r typecheck`, `pnpm -r lint`, `pnpm -r test`, `pnpm -r build`. The build emits `.output/public/manifest.webmanifest` and `.output/public/sw.js` (21 precache entries, 204.64 KiB).

### Pending — maintainer manual verification

The DoD items below cannot be exercised from this branch alone; they need a real browser and real device:

- [ ] Android Chrome shows "Add to Home Screen" against a deployed `pnpm preview` build.
- [ ] iOS Safari launches `dbboard-web` in standalone display mode with the icon on the home screen.
- [ ] Lighthouse PWA score ≥ 90 against a production build.
- [ ] Cold start while offline serves `/offline` from the precache with no white screen.

Tag the issue closed once these pass, and send the handback brief to the desktop side per the Handback section above.
